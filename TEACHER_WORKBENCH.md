# 教师教学工作台：需求与交付验收

状态：需求已确认，M2 学生管理基础、M3a 学生作业与考试分析草稿已实现，其余工作流分阶段推进。本文件不是全部功能的完工声明。

2026-09-08，用户确认第一版为“单个老师管理多个学生，本地或可信私网使用”。目标是把备课、上课后的记录、考试分析和家长沟通连接起来，减少重复录入，让每个学生的教学安排有真实记录可依据。

同日用户明确允许全面调整项目框架。M2 解决教师身份、学生建档和持久保存；本轮按用户最新要求推进 M3a，让所选学生的作业与考试资料生成可编辑、可复核的分析记录。用户确认资料同时包含照片/扫描件及可提取文字的文档。M1 的教材来源约束尚未接通。

## 本轮可用范围

- `/teacher` 提供学生名单、昵称搜索、在读与归档切换、基础资料卡、新建、编辑、归档与恢复。首页提供入口；“备课与课件”连接原有工作台。
- 新学生只需昵称。年级、考试年份、地区可留空，教材继续显示未确认和通用课程。不自动继承旧任务的初三、2027 年信息，不生成虚假成绩或掌握结论。
- 档案使用现有 StudentProfile 和 RuntimeStore，数据库使用现有 PostgreSQL persistence。教师名单只保存登记关系，个人资料仍使用原画像记录；没有另建上传或持久化系统。
- 教师身份沿用既有 owner，活动时续期 365 天，支持下载恢复码并在其他浏览器恢复访问。教师资料和名单不能从通用 persistence 接口读写。
- 创建请求支持幂等重试，更新检测版本冲突，归档保留历史记录。未改动字段的 inferred/confirmed 状态和证据不会因归档自动升级。
- 提供本地初始化与数据库备份/恢复命令。没有模型配置时仍可管理学生；本轮没有验证真实模型、OCR 或 PPT 内容质量。

资料卡现已接入“作业与考试分析”：老师指定学生、日期和资料用途，生成带原文依据的 AI 草稿，修改后保存并明确复核。课堂观察可作为本次分析的补充备注；独立的课次登记、逐题正式评分、跨次趋势、同卷学生差异和家长反馈尚未接通。

## M3a：学生作业与考试分析

1. 打开 `/teacher`，选择学生，点击“新建分析”。填写作业/月考/期中/期末等类型、学科、标题和日期，可补充本节课堂观察。
2. 使用原有 Materials 上传入口上传，或从自己的材料库选择资料。明确区分学生作答、题目/空白试卷、参考答案/评分标准、教材/教学资料；至少一份资料应为该学生的实际作答。
3. 点击“生成分析”。资料经所有权、文件完整性和大小校验后提取；仅把本次选定的内容和备注交给配置的分析模型，不附带其他学生或整份历史档案。
4. 草稿区分学习内容、可观察表现、需要练习、仍待核对，给出相应教学建议与证据限制。每条观察引用保存的原文段落；PDF 文字提取保留实际文件页码，不能把它称为教材印刷页码。
5. 老师可逐条修改分析及后续安排。先“保存草稿”，再“确认复核”；已复核内容再次编辑会回到草稿。可以撤回分析，原始草稿、提取快照和历史版本仍保留，撤回后不可继续修改。

纯文本、Markdown、CSV 和有文字的 PDF 可本地提取；照片、扫描/混合 PDF 及 Office 文件按已有文档提取器支持范围，使用老师选定或服务器配置的识别服务。照片和手写公式的识别质量依赖该服务，需对照原件核对。没有可用识别服务会提示配置，不伪造识别结果，也不擅自改用未选择、未配置的外部服务。Word 等 Office 文件通常需要支持对应格式的解析服务，不能仅因文字可选就承诺已有本地解析器。

分析模型沿用现有模型设置。服务器可通过 `MODEL_ROUTES` 的 `teacher-student-analysis` 条目固定该步骤的模型；未指定时沿用客户端选择及 `DEFAULT_MODEL`。老师可从分析窗口进入“模型与识别设置”。未配置可用模型时仍能管理学生、读取和修改已有分析；无法生成新的 AI 草稿。

无参考答案时，报告标为证据待补充，表现/练习候选归入待核对。有参考答案标签不等于答案可靠；模型解释和答卷对应仍需老师核实。来源匹配校验保证引文存在，不保证语义理解或主观题批改正确。报告不自动创建 StudyAttempt，不更新原有考试评分或独立掌握证据，也不输出精确能力百分比或考试分数预测。

沿用 RuntimeStore 和 PostgreSQL，新增的分析 kind 仅允许教师接口访问，通用 persistence 接口不可读取、伪造或删除。生成按 requestId 幂等，编辑使用版本冲突检测，老师与学生双重分区。当前单次上限为 6 份资料、每份 20 MiB、合计 40 MiB、提取文字合计 40,000 字符，每名学生最多 100 份分析；超限应拆分资料或后续扩展归档容量。历史版本不覆盖原始分析。

生成等待上限为 120 秒，可取消。底层不支持取消的第三方识别请求可能继续结束，但不会在取消后继续生成或保存分析；如取消与最终保存同时发生，关闭后先刷新历史记录，再决定是否重试。请求结果不明确时复用本次提交标识，避免重复归档。

分析专项浏览器测试使用独立的 `3001` 测试应用、真实 Materials/文件提取/PostgreSQL 和本地固定模型响应，不调用真实模型厂商。先构建项目，再运行：

```powershell
$env:TEACHER_E2E_ENABLED='1'
$env:TEACHER_E2E_BROWSER_CHANNEL='chrome'
pnpm exec playwright test --config e2e/teacher-analysis.config.ts
```

该测试不修改日常 `3000` 应用的模型配置，不把固定测试输出显示为真实 AI 的质量验收。

### M3a 验证记录（2026-09-08）

| 检查 | 结果 |
| --- | --- |
| 教师/分析模块、材料权限、模型路由、persistence、原画像/runtime 回归 | 23 个文件，365 项通过 |
| 真实 Chrome + Materials + PostgreSQL | 4 项通过：原学生管理 2 项、分析专项 2 项；专项模型为本地固定响应 |
| 分析专项操作 | 真实文本/PDF 上传、PDF 文件页码、可见原文引用、编辑、先保存后复核、原始草稿保留、撤回、刷新持久化及跨学生/老师隔离 |
| 失败与重试 | 模型错误和未配置 OCR 均不产生伪造报告；同一已保存请求重试只保留一份分析 |
| 布局 | 桌面 1440、手机 390/320 像素截图已检查，无页面水平溢出 |
| ESLint、定向 Prettier、i18n key 检查 | 通过 |
| 生产构建 | 通过；保留原有 Windows standalone 复制、工作区路径和 Edge 警告，采用完整项目目录运行 |
| 全项目 TypeScript | 新增类型错误清零；此前 4 项考试测试错误保留，未改动该基线 |

验收后日常工作台继续运行在 `http://127.0.0.1:3000/teacher`；专项测试的 `3001` 应用与固定模型已停止。未新增生产依赖，未修改 `.env.local`。没有调用真实模型厂商或真实 OCR 服务，因此不宣称已验证真实手写识别和教学分析质量。照片/扫描件的配置选择与提取分支通过模拟提取器测试，真实浏览器验证了无配置时的明确提示。并发的同一新请求可能各自触发模型调用，CAS 保证只有一份分析归档；尚未提供跨进程模型调用去重。

2026-09-08 GitHub 合并准备补充：修复此前保留的三处考试版本 fixture 类型错误、测试事件分支缺失，以及考试错因建议/复核链路混用 `gradingRef` 和 `assessmentRef` 的问题。考试相关 14 个文件 393 项测试通过；存储包类型检查通过，1017 项测试通过，119 项真实 PostgreSQL 等条件测试跳过。存储包因本分支公开 API 变化升至 0.27.0。上方 M2/M3a 验证表保留各阶段当时的结果，不代表这四项类型错误仍存在。教师工作台仍未提供正式考试复核 UI、跨次趋势或家长反馈。

## 启动与备份

前提：Node.js 22、已安装项目依赖、已启动 Docker Desktop。

```powershell
pnpm teacher:setup
pnpm dev
```

在启动地址打开 `/teacher`。初始化使用独立 Docker Compose 项目 `openmaic-teacher-local`，数据库默认只监听本机 `127.0.0.1:55432`，不会操作其他项目的数据库。首次配置其他端口使用 `pnpm teacher:setup --port 55433`；已有 `.env.local` 会被保留，后续沿用同一端口。生产模式使用 `pnpm build` 后运行 `pnpm start`。

在“工作区恢复”下载并私下保管身份恢复码。数据库备份另行运行：

```powershell
pnpm teacher:backup
```

备份生成到 Git 忽略的 `data/teacher-backups/`，不覆盖旧文件。恢复码负责找回访问身份，数据库归档负责找回存储内容，两者不能互相替代。整机迁移还需备份 `.env.local` 和现有文件存储目录/对象存储；数据库归档不包含外部上传文件。

恢复前停止应用并备份当前数据库。仅对本项目的可信归档使用以下命令，`--replace` 明确表示覆盖数据库中的同名对象与记录：

```powershell
pnpm teacher:restore --file data/teacher-backups/你的备份文件.dump --replace
```

恢复采用单事务，失败回滚；之后重新启动应用并按需恢复教师身份。本轮已在同容器独立临时数据库验证归档导入，未覆盖工作台数据库。详细本地说明见 `docs/teacher-local.md`。

真实浏览器回归需要先启动上述独立部署，使用虚构学生：

```powershell
$env:TEACHER_E2E_ENABLED='1'
$env:TEACHER_E2E_BROWSER_CHANNEL='chrome'
pnpm test:teacher:e2e
```

安装了 Playwright Chromium 时可不设置浏览器 channel。该测试不调用真实模型；默认不录制包含恢复码的网络 trace。测试专用浏览器身份与正常使用的老师身份分开。

## M2 验证记录

2026-09-08 完成以下验证，全部使用虚构学生和独立本地数据库：

| 检查 | 最终结果 |
| --- | --- |
| 教师模块、身份、persistence、原画像/runtime/伴学回归 | 14 个文件，276 项通过；限制 2 个 worker，测试超时预算 15 秒 |
| 真实 PostgreSQL + Chrome 端到端 | 2 项通过；包括创建幂等、教师隔离、未知字段、版本冲突、不同字段合并、归档恢复、换浏览器恢复身份 |
| 桌面与手机布局 | 1440、390、320 像素宽度验证；长昵称换行，页面无水平溢出，品牌图片正常加载 |
| 数据库重启 | 重启前后已存在的 12 条 runtime 记录保持一致 |
| 数据库备份 | 首次恢复 20 张表；最终包含虚构学生的备份恢复后，runtime_sessions 13 行、runtime_records 25 行均与源库一致；临时库已删除，工作台库未被覆盖 |
| ESLint、定向 Prettier、i18n key 检查 | 通过 |
| 生产构建 | 命令成功；本机 `next start` 的教师流程已实测通过 |
| 全项目 TypeScript | 本轮新增错误已清零；保留改造前已有的 4 项考试测试错误 |

Windows 构建出现工作区路径、Edge API 和 standalone 文件复制警告，本轮没有验证独立目录可直接发布或 Docker 应用镜像。当前验收使用完整项目目录运行，数据库单独由 Docker 托管。

保留的类型错误是 `tests/zhongkao/exam-error-review-private.test.ts` 的三个版本字段及 `tests/zhongkao/exam-service.test.ts` 的事件穷尽分支。改造前全套单测的 151 项失败及归因见审查快照，本轮没有重跑或宣称通过整个 8,000 余项测试集。

## 四条日常流程

| 流程 | 老师提供 | 系统协助 | 最终产物 |
| --- | --- | --- | --- |
| 备课与 PPT | 教材、考点文件、已有课件、教学主题和要求 | 检索资料、梳理对应考点、提出教法建议、编排课件、按要求修改 | 可编辑与导出的 PPT、教学建议、可回查的来源 |
| 课后与作业 | 本节实际教学内容、学生作答、课堂观察、作业情况 | 整理记录、辅助批改、建议补充练习、更新学生卡 | 每次课记录、修改后的作业、学生资料卡 |
| 阶段考试 | 月考/期中/期末试卷、每个学生答卷、答案或评分规则 | 辅助识别与核对、按题分析、纵向跟踪、找共性与个体差异 | 考试记录、个人分析、老师可见的差异清单、后续教学建议 |
| 家长反馈 | 老师选择的学生和时间段，以及上述已核对记录 | 整理学习内容、进展证据、待解决问题和家庭配合建议 | 可编辑的家长反馈草稿、单个学生的课后学习卡 |

“修改作业”同时覆盖批改已有作业和根据课堂表现调整后续作业。二者分别保留原始作答与新布置内容，不能因修改题目覆盖学生已提交的答案。

## 1. 基于教材和考点备课

### 资料如何发挥作用

资料仍通过现有 Materials 上传与解析系统进入。老师标明资料用途：教材、考点/考试范围、参考试题、已有 PPT 或其他补充材料。材料角色是老师的分类，不自动证明出版信息、地区政策或真实性。

每次备课选择具体资料和教学范围，系统先建立本课的对应关系：

- 本课学习目标和课时安排。
- 选定教材片段、考点要求及可回查的位置。
- 核心概念、先备知识、例题、练习和课堂检查点。
- 哪些内容来自资料，哪些是老师补充，哪些是 AI 自编或建议。
- 来源冲突、缺少答案、OCR 不清或尚未确认的事项。

老师提供并确认具体教材/考点后，系统按该范围工作；教材版本未知时继续使用 generic 模式。资料上传一次并保留可检索来源，不声称模型已经永久学会或记住整套教材。

PDF 文件页索引与教材印刷页码是不同字段，只有可靠识别并核对后才能标注印刷页码。文件名中出现出版社或年份不自动成为已确认教材身份。

### PPT 生成与修改

系统能先给出大纲与教学建议，再生成课堂可用课件。老师可以要求“把第 4 页拆成两页”“减少文字”“换一个同考点例题”“增加一道出口检测题”或“保留版式，降低例题难度”。

修改继续作用于现有课程/幻灯片模型，保留修改前后的版本依据，复用现有编辑器和 PPTX 导出。来源与教师讲解宜放在老师可查看的备课信息中，不把所有来源元数据塞满学生投影页。教师答案和评分参考不能混入学生练习版。

### 验收

1. 选定一份虚构教材片段和一份考点要求，可以生成与所选范围相符的课件大纲及课件。
2. 老师能回查课件所依据的材料片段；未提供的教材章节、地区政策或页码不会被补造。
3. 自编题和教学建议可识别，不被标为教材原题或官方考点。
4. 老师修改指定页后，未指定的内容保留，PPTX 可正常打开并继续编辑支持的元素。
5. 没有教材、资料尚未解析完成、识别失败或来源冲突时，有明确状态，不能假装已经按教材完成备课。
6. 越权或伪造的材料 ID、与原文不匹配的引用不能进入课件来源；资料版本变化后，旧引用需要重新核对。
7. 普通课程生成、原有学生练习及 PPTX 导出仍通过相应回归检查。

## 2. 学生资料卡、课堂记录与作业

### 学生资料卡

每个学生使用稳定 `profileId` 和昵称。老师身份负责访问权限，学生 ID 负责区分同一老师下的不同学生；二者不能混为一谈。昵称相同也不能合并档案。

资料卡展示：

- 老师录入的年级、考试年份、地区及各科教材，可留空或标为待确认。
- 最近教学内容、作业安排和考试记录。
- 有依据的进展、待观察事项，以及老师下一次课的安排。
- 每条重要结论的日期和相关作答/记录入口。

系统可以提供初三、2027 中考备考模板，但不把旧任务中一个学生的已知事实复制给全部学生。新建零记录学生时展示“探索计划”，不显示臆造的薄弱点或精确能力百分比。

### 课后登记

一节课可以共用课件、教学主题和布置的基础作业；老师逐个登记学生的实际完成情况。相同教学内容支持批量关联，但个人表现、是否使用提示、作答结果和评语不能自动批量复制。

系统区分以下记录：

| 记录 | 示例 | 可以支持的结论 |
| --- | --- | --- |
| 老师课堂观察 | “讲解时能复述步骤，独立运用还没测” | 可进入课堂备注，不能直接判定掌握 |
| 学生实际作答 | 某题答案、批改结果、提示使用情况 | 经校验形成作答证据 |
| AI 建议 | “下次可安排一道去括号迁移题” | 待老师采用的教学安排 |
| 老师确认事实 | 核对答卷后确认该题得分 | 说明事实已核对，不代表知识已掌握 |

作业批改保留原题、学生原始答案、评分依据和老师修正。AI 无可靠评分依据时输出待核对，不制造确定的对错。根据课堂情况可以减量、替换题目或安排不同练习，但不能凭一次错误把学生长期标为薄弱。

### 验收

1. 一个老师可创建和切换两名虚构学生；刷新或重启后资料仍能找到。
2. 为其中一人录入课堂表现、作业和提示使用情况，另一人的资料卡不改变。
3. 仅写入“听懂了”的老师备注不会增加独立掌握证据。
4. 改变后续作业不会改变历史提交；重复保存同一记录不会重复累计学习证据。
5. 长期使用不因固定 30 天 Cookie 到期而丢失访问身份；提供明确的备份和恢复路径。

## 3. 月考、期中、期末与差异分析

一份试卷可以被同一老师的多个学生使用，但每份学生答卷必须明确匹配到一个学生、一次考试和具体题目。扫描件包含多人时先分开核对，识别出的姓名不能未经确认就成为匹配依据。

录入需要区分试卷、学生答卷和答案/评分标准。只有空白试卷时，可以整理题型和涉及知识，不能凭试卷推断某个学生“不会什么”。现有确定性评分适用范围之外的题目由老师核对，不在第一版承诺全学科主观题自动准确批改。

老师的考试视图包含：

- 单次考试：逐题结果、原答卷依据、待复核项和下一步练习建议。
- 学生纵向变化：同知识点多次证据的变化，保留不同试卷难度、范围及满分差异。
- 同卷学生差异：共同未完成的题目、不同学生各自需跟进的题目和教学安排。
- 全班或小组共性：帮助安排下一次教学，不把内部比较直接发布给家长。

区分“结果现象”和“错因解释”：例如漏答或选项不一致可以有直接证据；“粗心”“焦虑”“概念没掌握”不能仅凭答案推断。AI 错因建议先是候选，由老师核对；即使确认一个错因，也不自动判定长期薄弱。

### 验收

1. 同一份试卷关联两名虚构学生的不同答卷，得到不同且可追溯的个人结果。
2. 无答案或评分标准的题目显示待核对，不能生成虚假的确定得分。
3. 知识点和错因候选需要经过既有复核边界；一次错误不直接判弱。
4. 不同考试之间保留可比性限制，不用原始总分直接制造能力增长百分比或中考分数预测。
5. 老师纠正学生匹配或删除考试后，重新计算的资料卡与反馈不再使用失效证据。

## 4. 有依据、可执行的家长反馈

家长体验来自看得见的教学工作：孩子学了什么、做到了哪一步、还有什么需要验证、老师如何继续教，以及家长如何配合。每次反馈应具体且适度，不靠夸大提升或制造焦虑表现价值。

反馈默认是一份供老师编辑的草稿，结构包括：

1. 本次学习内容：具体主题和课堂目标。
2. 可观察的进展：哪项作答、复述或迁移任务提供了依据。
3. 尚需练习或验证：准确描述现象，避免把“尚未测到”写成“不会”。
4. 老师已做和将做的工作：本次针对性讲解、作业调整、下次复查。
5. 家庭配合：一至两个可操作、时间负担明确的动作，不要求家长代讲整节课。

示例，仅用于说明格式，以下学生和记录均为虚构：

> 小禾今天练习了一元一次方程的去括号与移项。课末两道检查题中，一道独立完成，另一道在提示负号处理后完成，因此这一部分还需要继续验证独立运用。老师已经把后续作业调整为少量同类变式题，下次课会先用一道新题复查。家里可以请孩子用 3 分钟解释一道题为什么要变号，先听他完整说完；解释不清的地方标记给老师即可。

示例中的提示次数、题数和后续安排，在实际输出时都必须来自选定记录或老师确认的计划。没有记录时明确缺少依据，不自动补齐“进步明显”等评语。

### 验收

1. 选择学生和课程/时间范围，生成仅基于该范围记录的反馈草稿。
2. 家长可读正文不出现其他学生的身份、成绩、答卷或老师内部比较。
3. 提示后答对与独立完成的描述一致；老师备注与实际作答证据的措辞有所区分。
4. 数据不足时不编造进步、分数预测或升学概率。
5. 老师可以修改并确认草稿；修改底层成绩/记录后，旧草稿标为需要复核，已导出的版本保留生成时间，不声称已同步更新。
6. 可以导出或复制正文供老师使用；未获得明确发送指令，不自动联系家长。

## 已有代码与待补能力

| 能力 | 已核实的复用位置 | 本轮需求新增部分 |
| --- | --- | --- |
| 上传与资料读取 | `app/api/materials`、`components/workbench/compose-extras.tsx`、`lib/server/agent-runtime/material-tools.ts` | 教材/考点角色、本次备课来源选择与可回查关系 |
| 课件生成与修改 | `lib/server/agent-runtime/generation-tools.ts`、`course-tools.ts`、`course-edit/tools.ts`、现有工作台 | 以本课教材考点为约束的备课流程、教师/学生内容边界 |
| 教学技能 | `skills/agent-runtime/k12-core-literacy-planning`、`pro-editing` 等 | 教材考点来源与教学建议的明确区分，先解决 Windows 加载阻断 |
| PPTX 导出 | `lib/export/use-export-pptx.ts` | 来源和教师信息的适当呈现，不重复建设导出器 |
| 学生与证据 | `lib/zhongkao/profile.ts`、`runtime.ts`、`study-attempt.ts`、`progress.ts` | 老师下的学生名单与建档、资料卡、独立的课堂记录与作业管理 |
| 考试流程 | `lib/server/zhongkao/exam-*.ts`、现有考试 API | 修复真实引用链，接通老师录入复核界面、同卷关联与差异展示 |
| 家长反馈 | 现有生成 runtime 和 UI 可作为执行/编辑基础 | 选择证据、限制学生范围、草稿与复核、导出和失效提示 |

这里的 `curriculum-tools.ts` 是课程与文件夹编排工具，不应误认为现成的官方教材/考点知识库。现有 `roster-tools.ts` 也不能仅因名字含 roster 就被当作真实学生名单模块。

当前 `generate_scene` 的 `materialFacts` 只是可选字符串列表，没有结构化绑定教材、考点或引用选区。备课里程碑需要把可验证的来源带入生成契约，不能仅新增一段要求模型遵守的提示词。现有材料读取以提取文本和字符位置为主，可先保存材料 ID、提取版本/摘要、原文片段和字符定位；不宣传成已经具备全库语义检索或模型微调。

继续使用当前 persistence，不另建数据库、上传系统或独立文件存储。现有 owner 派生的 `learnerKey` 是访问分区，不等于真实学生 ID；同一老师下以 `profileId` 区分学生。上述逻辑关系先落实到既有存储和边界，再评估是否需要新增 app-defined record kind，不能在需求文档中宣称数据库迁移已经完成。

## 单里程碑交付顺序

| 里程碑 | 可验收的交付 | 前置条件 |
| --- | --- | --- |
| M0：范围与规则对齐 | 本需求文件与更新后的 AGENTS.md | 本次完成；没有把功能标为已实现 |
| M1：教材考点备课 | 选资料 -> 教学建议/大纲 -> 生成课件 -> 指定页修改 -> 导出 PPTX | 修复相关 Windows 技能加载阻断，复用既有材料与课程功能 |
| M2：老师管理学生 | 已实现：稳定老师身份、学生名单、零画像建档、基础学生卡和切换隔离 | 复用现有存储，提供本地部署及身份/数据库备份路径 |
| M3a：作业与考试分析草稿 | 已实现：学生绑定资料、提取原文、AI分析草稿、编辑复核及历史记录 | M2；复用 Materials、提取器、模型设置和 RuntimeStore；不代替正式评分/掌握证据 |
| M3：课后与作业 | 课堂记录、实际作答、作业批改及调整进入对应学生档案 | M2；保留实际证据与观察的区别 |
| M4：单次课家长反馈 | 从一名学生的一节课及相关作业生成可编辑、可回查草稿 | M3；先打通课后沟通，不等全部考试功能完成 |
| M5：单次考试录入分析 | 同卷多答卷的学生匹配、评分核对、个人分析 | M2；先修复现有 grading/assessment 引用错误及被阻断测试 |
| M6：阶段差异与周期反馈 | 跨课次/考试的差异视图及阶段家长反馈 | M4、M5；具备足够可比证据 |

每次实现一个里程碑，并为改变的行为添加测试。需要进一步拆小时，以能通过实际界面走通的最短流程为边界，避免继续堆积没有入口的领域代码。

## 保留的边界

- 第一版没有多老师协同、机构多租户、家长账号、收费管理或自动群发。
- 不要求学生真实姓名、学校、手机、身份证或地址；按昵称登记即可。
- 开发和验收只使用虚构学生、题目和记录。家长反馈示例不能混入真实档案。
- 本地/私网描述的是部署与存储范围；使用远端模型时，仅发送当前任务必要的选定资料，不把所有学生信息默认放进一次请求。
- 原有学生伴学的逐步提示、迁移验证、未知教材 generic、不得虚构来源和不得预测升学结果等规则继续适用。
- 老师可获得完整答案和备课建议，不代表学生端可以绕过提示门禁；教师查看答案也不产生学生已查看答案的事实。
- 本轮已改变教师身份、学生档案和相关存储边界。改造前审查记录保留于 `docs/reviews/project-review-2026-09-08.md`；其中 Windows 技能加载、考试 grading/assessment 引用链、媒体代理与考试测试门禁问题没有在 M2 中修复，不能声称全项目检查已经全绿。
